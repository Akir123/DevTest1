spring.datasource.url=jdbc:mysql://localhost:3306/patient_db
spring.datasource.username=root
spring.datasource.password=25051999
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
# Change the default location of templates (if your HTML files are not in the 'templates' directory)
spring.thymeleaf.prefix=classpath:/templates/

# Change the suffix (if you use something other than '.html')
spring.thymeleaf.suffix=.html


