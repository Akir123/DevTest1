server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Akir123/configuration_properties
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_PAT}
          clone-on-start: true
